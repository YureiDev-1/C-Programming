#include <stdio.h>
    int main() {
        int a,b;
        printf("Enter two integers: ");
        scanf("%d %d", &a, &b);

        printf("After swapping: a = %d, b = %d", b,a);

        return 0;
        
    }